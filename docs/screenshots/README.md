# docs/screenshots

Two images are referenced by the root README:

- `popup.png` — the popup mid-utterance, meter lit
- `in-page.png` — the listening bar over a compose window

Both are the same captures used for store screenshots 1 and 2. Keep them under
600 KB each so the README stays quick to load.
